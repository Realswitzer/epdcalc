import { Enemy } from "../types";

export interface Debuffs {
  rad?: number;
  scorch?: number;
  frostbite?: number;
  shred?: number;
  stun?: number;
  bleed?: number;
  burn?: number;
  rupture?: number;
  // first_strike, implement lengths
}
